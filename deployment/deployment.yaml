apiVersion: apps/v1
kind: Deployment
metadata:
  name: facemask-api
  namespace: facemask

spec:
  replicas: 1

  selector:
    matchLabels:
      app: facemask-api

  template:
    metadata:
      labels:
        app: facemask-api

    spec:
      containers:
      - name: facemask-api
        image: ngdangkhoa/facemask-api:v1

        imagePullPolicy: Always

        ports:
        - containerPort: 8000